# Inbox

Drop new, not-yet-ingested source files here (Markdown preferred), then tell your
vault agent to "process the inbox." After ingest, files move up to `sources/`.

This README stays put — it is not a source.
